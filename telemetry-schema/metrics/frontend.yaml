groups:
  - id: metric.frontend.requests
    type: metric
    metric_name: app.frontend.requests
    instrument: counter
    unit: requests
    brief: Total number of frontend requests
    stability: stable

