groups:
  - id: service.product_reviews
    type: attribute_group
    brief: Product reviews service attributes
    stability: stable
    attributes:
      - ref: app.product.id
      - ref: app.product.question
      - ref: app.product_reviews.count
      - ref: app.product_reviews.average_score

