groups:
  - id: service.cart
    type: attribute_group
    brief: Cart service attributes
    stability: stable
    attributes:
      - ref: app.cart.items.count
      - ref: app.user.id
      - ref: app.product.id

