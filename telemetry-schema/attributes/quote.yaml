groups:
  - id: registry.quote
    type: attribute_group
    brief: Quote service attributes
    stability: stable
    attributes:
      - ref: app.quote.items.count
      - ref: app.quote.cost.total

